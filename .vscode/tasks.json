{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Executar o projeto com novas alterações",
            "type": "shell",
            "command": "rm Gemfile.lock",
            "command": "bundle install",
            "command": "bundle exec puma config.ru --bind=tcp://0.0.0.0:9292",
            "problemMatcher": [
                "$lessc"
            ]
        }
    ]
}